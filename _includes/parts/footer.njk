<div class="mt-10 px-4 sm:px-20 py-10 ">
    <div class="container mx-auto">
        <div class="text-xs font-mono" >
            <p>&copy;{{site.copyYear}}
                {{site.author}}
                {% for link in site.footerLinks %}
               |
               <a href="{{link.url}}" {% if link.newTab %}
                   target="_blank"
               {% endif %}/>{{link.name}}</a>
            {% endfor %}
        </p>
        <p>Powered by <a href="https://www.11ty.dev/" target="_blank">eleventy</a>
        </p>
    </div>
</div>
</div>